export function updateServerTable(servers) {
    const tableBody = document.querySelector('.server-table tbody');
    if (!tableBody) return;

    tableBody.innerHTML = '';
    servers.forEach(server => {
        const row = createServerRow(server);
        tableBody.appendChild(row);
    });
}

function createServerRow(server) {
    const row = document.createElement('tr');
    row.innerHTML = `
        <td>${server.name}</td>
        <td>${server.ip_address}</td>
        <td>${server.type}</td>
        <td><span class="server-status status-${server.status.toLowerCase()}">${server.status}</span></td>
        <td>
            <div class="server-actions">
                <button class="action-btn" title="Bearbeiten">
                    <svg viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                        <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                    </svg>
                </button>
                <button class="action-btn" title="Neustarten">
                    <svg viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M23 4v6h-6"></path>
                        <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
                    </svg>
                </button>
                <button class="action-btn" title="Löschen">
                    <svg viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="3 6 5 6 21 6"></polyline>
                        <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
                        <line x1="10" y1="11" x2="10" y2="17"></line>
                        <line x1="14" y1="11" x2="14" y2="17"></line>
                    </svg>
                </button>
            </div>
        </td>
    `;
    return row;
}

